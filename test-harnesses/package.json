{
  "name": "@browser-mcp/test-harnesses",
  "version": "1.0.0",
  "description": "Test infrastructure with real browsers (Playwright)",
  "type": "module",
  "scripts": {
    "build": "echo 'No build needed for test harnesses'",
    "test": "vitest",
    "test:browser": "vitest --run browser-instances"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@types/node": "^20.10.0",
    "playwright": "^1.40.0",
    "typescript": "^5.3.3",
    "vitest": "^1.0.0"
  }
}
